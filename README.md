# SafeKey - Менеджер паролей

Кроссплатформенное настольное приложение для безопасного хранения и управления паролями.

## Функции

- 🔐 AES-256 шифрование всех данных
- 📁 Структурированное хранение с категориями (до 3 уровней)
- 🔍 Быстрый поиск и фильтрация
- ⌨️ Системный оверлей с горячими клавишами
- ☁️ Облачная синхронизация (Google Drive, Яндекс.Диск)
- 🎨 Современный интерфейс с темной/светлой темой

## Установка

```bash
npm install
```

**Важно**: Для сборки `better-sqlite3` может потребоваться установка дополнительных инструментов:
- Windows: Visual Studio Build Tools
- macOS: Xcode Command Line Tools
- Linux: `build-essential` и `python3`

## Запуск в режиме разработки

```bash
npm run dev
```

## Использование программы

**Подробное руководство пользователя:** см. [USER_GUIDE.md](USER_GUIDE.md)

### Быстрый старт:

1. **Первый запуск:** Создайте мастер-пароль (минимум 8 символов)
2. **Добавление пароля:** Нажмите "Новый пароль" в боковой панели
3. **Быстрый доступ:** Нажмите `Ctrl+Shift+P` для открытия оверлея
4. **Генератор паролей:** Используйте кнопку "Сгенерировать пароль" при добавлении

## Сборка для Windows

```bash
# Сборка для Windows (x64 и ia32)
npm run build:win

# Сборка только для x64
npm run build:win64
```

Результат установщика будет в папке `release/`.

**Подробная инструкция по сборке:** см. [BUILD.md](BUILD.md)

## Структура проекта

```
SafeKey/
├── src/
│   ├── main/              # Главный процесс Electron
│   │   ├── config/        # Конфигурация приложения
│   │   ├── database/      # Работа с базой данных
│   │   ├── encryption/    # Шифрование AES-256
│   │   ├── types/         # TypeScript типы
│   │   └── main.ts        # Точка входа
│   ├── renderer/          # React приложение
│   │   └── src/
│   │       ├── components/
│   │       ├── hooks/
│   │       └── utils/
│   └── shared/            # Общие типы
│       └── types/
├── build/                 # Ресурсы для сборки
└── release/              # Готовая сборка
```

## Технологии

- Electron 28+
- React 18 + TypeScript
- SQLite (better-sqlite3)
- crypto-js для AES-256 шифрования
- Vite для сборки
- electron-builder для создания установщиков
