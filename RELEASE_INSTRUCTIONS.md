# 📦 Инструкция по созданию релиза v1.0.3

## ✅ Правильный способ (для автообновления):

### 1. Создайте установщик через Inno Setup:

1. Откройте файл `installer.iss` в Inno Setup Compiler
2. Версия уже обновлена до 1.0.3
3. Нажмите **"Build"** → **"Compile"** (или F9)
4. Установщик будет создан: `release\SafeKey-Setup-1.0.3-x64.exe`

### 2. Создайте Release на GitHub:

1. Откройте: https://github.com/Actoric/SafeKey/releases/new
2. **Choose a tag:** `v1.0.3` (уже создан)
3. **Release title:** `SafeKey v1.0.3`
4. **Description:** (см. RELEASE_V1.0.3.md)
5. **Загрузите файл:** `SafeKey-Setup-1.0.3-x64.exe` из папки `release/`
6. **Нажмите:** "Publish release"

## ⚠️ Важно:

- **Установщик (.exe) обязателен** для работы автообновления
- Архив (ZIP) не подходит для автообновления через electron-updater
- Файл `latest.yml` создастся автоматически при следующей сборке через electron-builder

## 🔄 Альтернатива (если нет Inno Setup):

Если не можете создать установщик сейчас:
1. Заархивируйте `release\win-unpacked\` в ZIP
2. Назовите: `SafeKey-1.0.3-x64.zip`
3. Загрузите в Release
4. **НО:** автообновление не будет работать до создания установщика

---

**Рекомендация:** Используйте Inno Setup для создания установщика - это правильный способ для автообновления!

