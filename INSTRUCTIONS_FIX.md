# Инструкция по исправлению проблем

## Проблемы, которые нужно исправить:

1. ✅ **Окно без элементов браузера** - исправлено (frame: false, кастомная TitleBar)
2. 🔄 **Мастер-пароль не обрабатывается** - добавлено логирование
3. 🔄 **Консоль пустая** - временно включены DevTools
4. 🔄 **Ярлык не использует иконку** - проверьте настройки electron-builder

## Что нужно сделать:

### 1. Пересобрать приложение

```bash
npm run build:win
```

### 2. Запустить и проверить консоль

1. Запустите `release\win-unpacked\SafeKey.exe`
2. **Консоль разработчика должна открыться автоматически** (временно включена)
3. Если не открылась - нажмите `F12`

### 3. Проверить логи

Введите мастер-пароль и посмотрите в консоль. Должны быть логи:
- `[Setup] Начинаем установку мастер-пароля...`
- `[useAuth] setup вызван...`
- `[Main] set-master-password вызван`
- и т.д.

### 4. Если логи есть, но UI не обновляется

Это проблема React - состояние обновляется, но компонент не перерисовывается.

**Временное решение:** Закройте и откройте приложение заново после установки пароля.

### 5. Отключить DevTools после исправления

В файле `src/main/main.ts` найдите строку:
```typescript
mainWindow.webContents.openDevTools();
```

И удалите или закомментируйте её.

## Что изменено:

- ✅ Окно без стандартных элементов браузера
- ✅ Кастомная панель заголовка с кнопками управления
- ✅ Добавлено подробное логирование всех операций
- ✅ Временно включены DevTools для отладки
- ✅ Исправлены пути к ресурсам (base: './')
