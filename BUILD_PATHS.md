# Директории сборки SafeKey

## Структура директорий после сборки

### 1. Промежуточная сборка: `dist/`

Сюда собирается скомпилированный код **до** упаковки в Electron:

```
dist/
├── main/              # TypeScript → JavaScript (главный процесс)
│   ├── main.js
│   ├── preload.js
│   ├── config/
│   ├── database/
│   └── encryption/
└── renderer/          # React → HTML/CSS/JS (интерфейс)
    ├── index.html
    └── assets/
```

**Создается командами:**
- `npm run build:main` - компиляция TypeScript (main процесс)
- `npm run build:renderer` - сборка React приложения (Vite)

### 2. Финальная сборка: `release/`

Сюда electron-builder упаковывает готовое приложение:

```
release/
├── win-unpacked/          # ✅ Распакованная версия приложения
│   ├── SafeKey.exe        # ⬅️ ЗДЕСЬ готовое приложение!
│   ├── resources/
│   │   └── app.asar       # Упакованное приложение
│   └── [другие файлы Electron]
│
├── SafeKey-1.0.0-x64.exe  # Установщик (если сборка успешна)
└── builder-effective-config.yaml
```

**Создается командой:**
- `npm run build:win` - полная сборка и упаковка

## Конфигурация в package.json

```json
"directories": {
  "output": "release",        // ← Финальная директория
  "buildResources": "build"   // ← Ресурсы (иконки и т.д.)
}
```

## Где находится готовое приложение?

### ✅ Распакованная версия (работает сразу):
```
E:\Progect\SafeKey\release\win-unpacked\SafeKey.exe
```

### 📦 Установщик (если сборка успешна):
```
E:\Progect\SafeKey\release\SafeKey-1.0.0-x64.exe
```

## Полный путь сборки

1. **Исходники** → `src/` (TypeScript/React код)
2. **Промежуточная сборка** → `dist/` (скомпилированный JavaScript)
3. **Финальная упаковка** → `release/` (готовое приложение)

## Важно

- Папка `release/` создается автоматически при первой сборке
- Папка `dist/` перезаписывается при каждой сборке
- Папку `release/` можно безопасно удалить и пересобрать
- Приложение готово к использованию из `release/win-unpacked/`
